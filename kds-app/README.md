# KDS App

React Native WebView wrapper for running KDS Web as an Android tablet or Android TV app.

This app intentionally stays thin. KDS UI lives in `kds-web`; this wrapper loads the configured KDS URL and provides device-level packaging.

## Later Scope

- React Native project initialization
- `react-native-webview`
- KDS Web URL settings
- Android APK build

